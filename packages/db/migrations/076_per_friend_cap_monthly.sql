-- ============================================================================
-- Migration 076: 友だち単位 AI 接客上限を「日次」→「月次」に変更
--
-- 075 では 24 時間あたりの上限 (per_friend_daily_cap) として導入したが、
-- 運用方針として「1 友だち / 暦月あたり 50 通」に変更。列名も実態に合わせて
-- per_friend_monthly_cap にリネームする。既存の値はそのまま引き継がれる。
--
--   NULL = 無制限
-- ============================================================================

ALTER TABLE tenant_metering
  RENAME COLUMN per_friend_daily_cap TO per_friend_monthly_cap;
