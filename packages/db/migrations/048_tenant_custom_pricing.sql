-- ============================================================================
-- Migration 048: tenant_metering にカスタム料金列を追加
--
-- 目的:
--   営業時に個別に決めた「月額料金 (運用代行費)」と「各配信枠」を
--   プラン固定ではなくテナント単位で自由に設定できるようにする。
--
--   既存の plan 列は残すが、UI 上はプラン選択を廃止し、すべて個別入力可能に。
--   既存テナントは plan のデフォルト値で初期化されているため、ここで
--   monthly_fee_yen は NULL を許容し、未設定なら UI で従来プラン料金を
--   default として提示する想定。
-- ============================================================================

ALTER TABLE tenant_metering
  ADD COLUMN monthly_fee_yen INTEGER;
