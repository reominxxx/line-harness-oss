-- ============================================================================
-- Migration 077: AI 接客が回答できない時の固定フォールバックメッセージ
--
-- AI が回答を生成できなかった場合 (生成エラー / タイムアウト / 出力ガード発動) に
-- 返す定型文を、テナントごとに「AI 配信設定」で事前に用意できるようにする。
--
--   NULL / 空文字 = 設定なし → 従来のシステム既定文を使う
-- ============================================================================

ALTER TABLE tenant_metering
  ADD COLUMN ai_fallback_message TEXT;
